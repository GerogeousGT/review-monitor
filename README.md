# review-monitor

Мультиагентная система мониторинга отзывов для сервисного бизнеса. Изначально — домашнее задание курса Zerocoder (тема: автономные и многоагентные системы, CrewAI), собирается сразу как реальный пилотный кейс на примере фитнес-клуба.

**Пилот:** World Class, ул. Республики, 245, Тюмень — площадки 2ГИС, Яндекс Карты, Zoon.

## Что делает

- Собирает новые отзывы с нескольких площадок: каждый прогон проверяет ограниченное недавнее окно (не всю историю — `max_scrolls`/`max_clicks` у Playwright-коллекторов, `lookback_days` у Apify/2ГИС), дедуп — на уровне БД (`UNIQUE(platform, external_review_id)`, `INSERT OR IGNORE`), не курсор-скрейпинг с ранним выходом (уточнено 2026-07-17 — `location_platforms.last_seen_review_id/date` пишется на будущее, но пока ни один коллектор его не читает)
- Определяет тональность **по темам внутри отзыва** (aspect-based), а не отзыва целиком — жалоба на бассейн внутри в целом позитивного отзыва не теряется. Каждая тема (тег) отделена от места (`zone`, необязательное поле) — "оборудование хромает в тренажёрке" даёт тег "оборудование" + zone "тренажёрный зал", а не тег зоны вместо темы, иначе одна и та же проблема размазывалась бы по разным залам и не набирала бы порог алерта (см. PLAN.md "Tag + Zone")
- Копит статистику в БД и поднимает персистентный жёлтый/красный алерт, если однотипных жалоб становится много за настраиваемое окно времени (не разовое уведомление — алерт живёт, пока не закрыт вручную)
- Следит за отзывами без ответа и напоминает, если SLA на ответ просрочен (сам определяет появление ответа — перечитывает страницу отзыва). Три эшелона по возрасту отзыва (`review_date`): до 90 дней — суточный watchdog, 90-180 дней — недельный обзор без срочного тона, старше 180 дней — нигде не всплывает, но остаётся `pending` в БД для статистики
- Пишет черновик ответа с учётом тона компании (документ или вытянутый из истории прошлых ответов) и присылает всё через Telegram — без автопостинга, решение всегда за человеком
- Раз в день шлёт сводку по всем открытым алертам (не только по изменившимся за сутки) — чтобы забытая проблема не потерялась
- Раз в неделю (понедельник утром) шлёт итоги для руководителя: объём отзывов за неделю и за месяц, топ хвалимого и топ ругаемого раздельно за месяц (за неделю обычно недостаточно данных — тема с 1 упоминанием не показывается как "топ"), список системного негатива (активные yellow/red алерты), просрочки по SLA за неделю
- Отдельно ловит "повторного негативщика" — один и тот же автор оставил несколько негативных отзывов НА ОДНОЙ площадке (author+platform, площадки не смешиваются — одинаковое имя на разных сайтах не гарантированно один человек). Порог кастомный под клиента (`alert_rules.repeat_offender` в конфиге). Напоминает раз в N дней, пока кто-то не нажмёт кнопку "Связался с клиентом" (Telegram-кнопка, мгновенная реакция через webhook) или не отметит через `main_ack.py` — история (кто и когда связался) остаётся в БД, видна в дашборде
- Отдельно ловит "проблемную зону" — не тема X копится (уже есть выше), а конкретное место (тренажёрный зал, раздевалка, кабинет) стабильно собирает разный негатив (то на оборудование, то на персонал, то на чистоту) — по отдельности ни одна тема не набирает порог, но зона в целом проблемная. Свободный текст `zone` от модели нормализуется против известных мест клуба ("женская раздевалка" → "раздевалка"), иначе счётчик дробится на варианты написания
- Когда модель встречает тему, для которой в словаре нет подходящего тега, — предлагает новый со своей категорией, но он не активен автоматически: уведомление в Telegram + approval в дашборде (утвердить / утвердить с другой категорией / отклонить), новый тег виден вместе с исходным отзывом целиком, чтобы можно было сразу поправить и другие теги на нём

## Структура (multi-tenant с 2026-07-08)

Один код обслуживает несколько клиентов одновременно — общий движок, разные конфиги/БД/боты:

```
main_*.py          — точки входа (команды), запускаются напрямую или по cron/systemd
core/               — инфраструктура: db, config, env (выбор клиента), LLM-провайдер,
                       dates (единый парсер дат — сглаживает разницу в поведении
                       между Python 3.10 на VPS и локальной версией, Z-суффикс,
                       часовые пояса — от этого зависит каждый collector)
agents/             — 4 из 5 компонентов архитектуры: Alert Engine, Sentiment
                       Analyst, Reply Strategist, Notifier
collectors/         — 5-й компонент (Collector), по файлу на площадку;
                       base.py — synthetic_id (стабильный хэш вместо нативного
                       review ID для площадок, где его нет, — дедуп держится на этом)
webapp/             — 6-й компонент, дашборд (Flask, единственный постоянно
                       работающий процесс в проекте — всё остальное здесь batch-
                       скрипты по расписанию). Своя авторизация/роли (admin видит
                       всех clients/*, client — только свой slug), задеплоен на
                       reviewpulse.ru через nginx. В разработке (v1) — см. PLAN.md
                       "Дашборд клиента". ВАЖНО: в отличие от main_*.py (CLIENT_SLUG
                       один раз на процесс), путь к БД клиента здесь резолвится
                       НА КАЖДЫЙ HTTP-запрос (см. app.py: _client_db_path) — не
                       полагается на core.env.get_client_root()/CLIENT_SLUG.
db/schema.sql       — общая схема SQLite (не клиент-специфична)
clients/<slug>/     — ВСЁ клиент-специфичное: client_config.yaml, tone_of_voice.md,
                       few_shot_examples.yaml (опционально, см. Golden eval ниже),
                       db/reviews.db (своя SQLite), .env (свой Telegram-бот, свой
                       Apify-токен — разный биллинг). Ничего из этой папки не
                       переиспользуется между клиентами.
tests/              — pytest (ключевая логика — пороги алертов, дедуп, БД) +
                       tests/golden/ (регрессионный тест LLM, см. ниже)
```

Выбор клиента — переменная окружения `CLIENT_SLUG` (обязательна, без неё любой
`main_*.py` падает с явной ошибкой, а не молча работает "не на том" клиенте):
```
CLIENT_SLUG=worldclass .venv/Scripts/python main_notify.py
```
или через обёртку: `./run_cycle.sh worldclass`.

- `clients/<slug>/client_config.yaml` — все настраиваемые под клиента параметры (площадки, теги, пороги алертов, SLA). Файл разбит на 3 блока: **КЛИЕНТ** (меняется под каждого нового заказчика), **ШАБЛОН ПОД НИШУ** (теги/категории — переиспользуется для той же отрасли), **ДВИЖОК** (технические дефолты, можно стартовать без изменений). Чек-лист разворачивания под нового клиента — в шапке самого файла.

## Новый клиент — быстрый разворот

Код общий, разворот — это просто новая папка `clients/<slug>/`, без копирования репозитория:
1. `mkdir clients/<новый_slug>` (+ `db/` создастся сама при первом запуске)
2. Скопировать `client_config.yaml` от похожего клиента, пройти чек-лист в его шапке
3. Написать/скопировать `tone_of_voice.md` под голос нового бренда
4. Завести `clients/<slug>/.env`: свой Telegram-бот (не переиспользовать чужого бота), свой `APIFY_API_TOKEN`
5. **Первый сбор истории — ОБЯЗАТЕЛЬНО с флагом:** `CLIENT_SLUG=<slug> python main_collect.py --backfill`. Без флага (или если позже понадобится доливать в БД старые отзывы вручную, в обход обычного цикла) следующий плановый `main_notify.py` разошлёт карточку по КАЖДОМУ вставленному отзыву одним пакетом — на практике это уже было 90 сообщений разом (см. CHANGELOG 2026-07-09)
6. Дальше — обычный цикл: `CLIENT_SLUG=<slug>` (или `./run_cycle.sh <slug>`) без флага
7. **Golden dataset** — `tests/golden/dataset_<slug>.yaml`, собрать ~20-30 реальных
   размеченных отзывов после первого сбора истории (не выдуманных — см. Golden eval
   ниже), прогнать эвал, откалибровать словарь тегов по расхождениям. Не обязательно
   для самого первого запуска, но обязательно перед тем, как доверять alert-порогам
   на новом клиенте — новый словарь тегов не проверен без этого
8. **`few_shot_examples.yaml`** — опционально, заводить только по факту находки
   систематической путаницы модели на golden eval (см. Golden eval ниже), не заранее
   "про запас" — добавление примеров без проверки эффекта чаще вредит, чем помогает
   (см. PLAN.md/CHANGELOG 2026-07-15/16)

Уже развёрнуто: `clients/worldclass` и `clients/daudelsport` — оба полностью в проде (не "в процессе", исправлено 2026-07-16 — статус годами отставал от факта, см. PLAN.md/CHANGELOG). Гипотеза "15-20 минут на разворот" подтверждена вторым клиентом фактически.

Подробности — в [PLAN.md](PLAN.md).

## Установка

```
python -m venv .venv
.venv/Scripts/pip install -r requirements.txt
.venv/Scripts/python -m playwright install chromium
```

Корневой `.env` — fallback для ключей, реально общих на всех клиентов:
```
PROXYAPI_KEY=...   # активный провайдер (см. core/llm_provider.py), GROQ_API_KEY — запасной вариант
```

`clients/<slug>/.env` — per-client, обязателен перед запуском на этом клиенте:
```
TELEGRAM_BOT_TOKEN=...   # свой бот под клиента, НЕ переиспользовать бот другого клиента
TELEGRAM_CHAT_ID=...     # узнать через getUpdates после /start боту
APIFY_API_TOKEN=...      # console.apify.com → Settings → Integrations, нужен для сбора 2ГИС; свой на клиента (разный биллинг)
```

Все команды требуют `CLIENT_SLUG` в окружении. Дальше по порядку:
`main_collect.py` → `main_analyze.py` → `main_reply.py` → `main_alerts.py` → `main_notify.py`.
`main_alerts.py` — единственное место, где пересчитываются И отправляются в Telegram
тег-/зона-алерты (исправлено 2026-07-17: раньше `main_notify.py` тоже вызывал пересчёт,
но он идемпотентен — второй подряд вызов в одном цикле видел пустой diff, поэтому
уведомления об алертах молча не уходили, см. CHANGELOG). `main_notify.py` теперь шлёт
только карточки новых отзывов.
Раз в день (`digest_time` в конфиге, systemd timer) — `main_digest.py` и `main_watchdog.py`.
Раз в неделю — `main_weekly_stale.py` (просрочки 90-180 дней, см. `watchdog_recent_days`/`watchdog_stale_cutoff_days` в конфиге) и `main_weekly_summary.py` (итоги для руководителя: объём неделя/месяц, топ хвалимого/ругаемого за месяц, системный негатив = активные алерты, просрочки за неделю — понедельник утром).
Раз в `renotify_after_days` (конфиг, обычно раз в 2 дня) — `main_repeat_offender_notify.py` (напоминание по открытым repeat-offender алертам с кнопкой "Связался", молчит для acknowledged).
Нажатие кнопки "Связался" обрабатывается webhook'ом (`webapp/app.py: POST /telegram/webhook/<slug>`, реализовано 2026-07-16 — заменяет прежний polling-скрипт, который так и не был развёрнут на VPS). Регистрация — `main_set_webhook.py`, вручную один раз на клиента после разворота бота.
Пометить алерт "в работе" вручную (для тег-алертов, у repeat offender есть кнопка) — `main_ack.py`.
Обёртка для всего цикла сразу: `./run_cycle.sh <client_slug>`.

Тесты: `.venv/Scripts/python -m pytest tests/` (не трогают реальные клиентские БД — изолированная SQLite в памяти).

## Golden eval — регрессионный тест Sentiment Analyst

Отдельно от pytest — платные вызовы LLM, гонять вручную при смене модели/промпта/провайдера,
не на каждый коммит:
```
CLIENT_SLUG=<slug> .venv/Scripts/python tests/golden/run_eval.py <slug>
```
У каждого клиента **свой** датасет (`tests/golden/dataset_<slug>.yaml`) и свой словарь тегов
(`clients/<slug>/client_config.yaml`) — нет общего эвала на всех клиентов, потому что дерево
тегов у каждого своё (даже у двух фитнес-клубов уже разное, у клиента из другой ниши — тем
более). Опционально — `clients/<slug>/few_shot_examples.yaml`, per-client примеры для промпта
(куратор — golden eval: добавлять только если реально поднимает accuracy на прогоне, не по
теории). Метрика — per-tag (каждый ожидаемый тег отдельно), порог принятия 90%. Подробности,
история калибровки и находки — PLAN.md/CHANGELOG.md.

## Статус

- [x] **Collector** — Яндекс.Карты и Zoon через Playwright, 2ГИС через Apify (residential-прокси — собственный Playwright не пробивал антибот даже с жилого IP, см. PLAN.md), идемпотентная запись в БД, определение наличия ответа компании. Все три площадки проверены на реальных данных.
- [x] **Sentiment Analyst** — LLM, aspect-based тональность (тег ≠ тональность всего отзыва, + `zone` отдельным полем — см. выше), управляемый словарь тегов с описаниями категорий, golden eval на каждого клиента (см. ниже). `main_analyze.py` НЕ доверяет флагу `is_new` от модели вслепую — сверяет с реальным активным словарём в коде, это защищает от ДУБЛЕЙ существующих тегов под новым именем. Полностью выдуманные новые теги попадают в словарь со статусом `pending_review` (не активны для будущих отзывов), с 2026-07-16 — approval через дашборд (см. ниже), не тихая утечка.
- [x] **Alert Engine** — три независимых типа алертов, все персистентные (open/acknowledged/resolved, авто-закрытие при возврате к норме): (1) **тег-алерт** — два сигнала, берётся ХУДШИЙ из двух (закрывает "слепую зону между вспышкой и хронической проблемой", см. CHANGELOG 2026-07-06): burst (`client_config.yaml`, несколько окон на тег, всплеск за короткий срок) + chronic tiers (`chronic_tiers`, тема тянется долго, даже если не всплесками); (2) **зональный алерт** (2026-07-16) — место, не тема, стабильно копит разный негатив, только burst-порог (chronic tiers для тем и зон — разная природа величин, не переиспользуются); (3) **repeat offender** — см. выше. Watchdog по просрочке SLA ответа. Ручное acknowledge — `main_ack.py` (CLI) или кнопка "Пересчитать алерты" в дашборде. Пересчёт **и отправка** тег-/зона-алертов в Telegram живут вместе в `main_alerts.py` (см. выше и CHANGELOG 2026-07-17) — колокировано намеренно, `recompute_*` идемпотентен, второй пересчёт того же состояния в другом скрипте не даёт diff. Проверено на синтетике (yellow/red/дедуп) и на реальных данных обоих клиентов.
- [x] **Notifier** — отправка в Telegram (свой бот на каждого клиента): карточка нового отзыва (включая черновик ответа и заметку для менеджера), тонкий пинг об изменении тег-/зона-алерта, дайджест просрочек по SLA, текстовое уведомление о новом теге на утверждение. Backfill (первоначальная история) не спамит уведомлениями — уведомляются только по-настоящему новые отзывы. **Разделение ответственности Telegram/дашборд** (закреплено 2026-07-17): Telegram — только сигнал "что случилось и куда идти" (карточка отзыва + черновик ответа — единственное исключение, это сам deliverable продукта, не аналитика), весь разбор (drill-down по алерту, история, тренды, ручные правки) — в дашборде. Алерт-пинг поэтому заканчивается фразой "Разбор/Подробности — в дашборде", без цитат/evidence в самом сообщении.
- [x] **Reply Strategist** — черновик ответа + классификация (`positive`/`конструктив`/`эмоциональный`/`требует_проверки`) по гайду тона (`tone_of_voice.md`, собран на методичках OpenUp «Директор ФК»). Контекст активных алертов — только во внутреннюю заметку, не в публичный текст. Проверено на всех 51 отзыве, ожидающем ответа — 0 "требует_проверки" (в реальных данных нет троллинга/чёрного PR).
- [x] **Dashboard (webapp/)** — 6-й компонент, задеплоен на reviewpulse.ru через nginx. Единственный постоянно работающий процесс проекта — остальное batch-скрипты по расписанию. Три вкладки: **Обзор** (сводка алертов всех типов, тренд тональности, топ хвалимого/ругаемого, скрытые проблемы), **Лента отзывов** (фильтры, ручная коррекция/добавление/удаление тега на конкретном отзыве, кнопка "Пересчитать алерты сейчас"), **Словарь тегов** (2026-07-16 — дерево активных тегов по категориям с описаниями, свёрнуто/развёрнуто по клику; approval-очередь новых тегов от модели — карточка с исходным отзывом целиком, утвердить/утвердить с другой категорией/отклонить). Авторизация с ролями (admin/client). План v2 (Health Score, автокластеризация, детекция аномалий) — PLAN.md "Дашборд клиента".
- [x] **Webhook для Telegram-кнопок** (2026-07-16) — `POST /telegram/webhook/<slug>` в `webapp/app.py`, переиспользует уже работающий Flask-процесс дашборда, не отдельный сервис. Обрабатывает нажатие "Связался с клиентом" на repeat-offender уведомлениях. Регистрация — `main_set_webhook.py`, вручную один раз на клиента.

Первые 5 компонентов оригинальной архитектуры (Collector → Sentiment Analyst → Alert
Engine → Reply Strategist → Notifier) собраны и проверены на реальных данных. Dashboard —
6-й компонент, добавленный сверх исходного домашнего задания, в процессе.

- [x] **Деплой на VPS** — работает на боевом сервере, systemd-таймеры (пайплайн раз в 6 часов без watchdog,
  watchdog и дайджест ежедневно, дайджест в 18:00 МСК, недельный обзор давних просрочек — раз в неделю),
  без ручного запуска. Именование юнитов — `review-monitor-<slug>[-<job>].timer`
  (например `review-monitor-worldclass.timer`, `review-monitor-worldclass-digest.timer`,
  `review-monitor-worldclass-watchdog.timer`, `review-monitor-worldclass-weekly-stale.timer`)
  — **per-client**, у каждого нового клиента свой полный набор таймеров, ничего не
  переиспользуется автоматически. Подробности — PLAN.md/CHANGELOG.md.

**Обновить прод после правок кода/конфига**: `git push`, затем `./deploy.sh` —
`git pull` на VPS + `pip install` + `pytest` + рестарт `review-monitor-webapp.service`
(единственный постоянный процесс, batch `main_*.py` подхватывают новый код сами
на следующий запуск по таймеру). БД (`clients/*/db/`) никогда не деплоится — в
`.gitignore`, живёт только на VPS. Деплой НЕ автоматический по push (осознанно —
пауза перед проверкой на реальном проде, см. CHANGELOG 2026-07-16).

## Полная документация

- [PLAN.md](PLAN.md) — что задумано, что сделано, что в очереди (обновлять по ходу, не забывать идеи)
- [CHANGELOG.md](CHANGELOG.md) — история сессий, технические решения и почему
