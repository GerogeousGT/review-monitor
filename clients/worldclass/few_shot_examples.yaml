# Примеры для few-shot в промпте Sentiment Analyst (agents/sentiment_analyst.py) —
# инфраструктура готова (core.config.load_few_shot_examples, {examples_block} в
# SYSTEM_PROMPT), но СЕЙЧАС пусто.
#
# Проверено ДВАЖДЫ на golden eval (2026-07-15 и 2026-07-16, второй раз — на честной
# per-tag метрике, после того как выяснилось, что первая проверка шла на сломанной
# all-or-nothing метрике): 2 примера, нацеленные именно на путаницы этого клиента
# (вентиляция vs zone, информирование как доп.тег), ОБА РАЗА дали регрессию —
# 89%→81% на правильной метрике (sentiment 100%→89%, per-tag 79%→73%). Это не
# артефакт измерения — реальный эффект. На gpt-4o-mini в этой задаче (27 тегов,
# тонкие границы) добавление примеров в промпт стабильно НЕ помогает, вопреки
# общей теории prompt engineering. См. PLAN.md/CHANGELOG 2026-07-15/16.
#
# Не добавлять примеры без предварительной проверки через golden eval на ПРАВИЛЬНОЙ
# (per-tag) метрике, что они реально поднимают accuracy, а не роняют его.
[]
