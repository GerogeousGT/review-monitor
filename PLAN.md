# PLAN — review-monitor

Живой документ: что задумали, что сделали, что в очереди. Обновляется по ходу работы —
не только "после того как сделали", но и в момент, когда придумали что-то новое и решили
отложить. Цель — не терять идеи и не путать "мы это обсуждали" с "мы это построили".

## Что это

Мультиагентный мониторинг отзывов для сервисного бизнеса. Домашка курса Zerocoder
(автономные/многоагентные системы), собирается сразу как реальный демо-кейс на примере
фитнес-клуба (World Class, Тюмень).

**Стратегический разворот (2026-07-06):** не только пилот для одного клиента — план
докрутить до аналитики и быстро разворачивать на несколько компаний (прислать им готовый
анализ отзывов как демо/лид-магнит). Отсюда — приоритет на быстрое пересобирание конфига
под нового клиента (см. ниже) раньше, чем на Docker (Docker — когда клиентов станет 2-3+,
см. "Идеи").

## Архитектура (5 компонентов)

```
[Scheduler] → [Collector] → [Sentiment Analyst] → [Reply Strategist]
                                    ↓
                            [Alert Engine] → [Notifier → Telegram]
```

| Компонент | Файл | Что делает |
|---|---|---|
| Collector | `collectors/*.py`, `main_collect.py` | Playwright, курсор по последнему отзыву, не пересканирует историю |
| Sentiment Analyst | `agents/sentiment_analyst.py`, `main_analyze.py` | LLM, aspect-based тональность, управляемый словарь тегов+категорий |
| Alert Engine | `agents/alert_engine.py`, `main_alerts.py` | Пороги по окнам ("вспышка") + ступенчатая эскалация ("тлеющая проблема", 180→360→720 дней), персистентное состояние, Watchdog по SLA |
| Reply Strategist | `agents/reply_strategist.py`, `main_reply.py` | Черновик ответа + классификация, гайд тона (`tone_of_voice.md`) |
| Notifier | `agents/notifier.py`, `main_notify.py` | Telegram, свой бот @RewvieWClass_bot |
| Дайджест | `main_digest.py` | Ежедневная сводка: все открытые алерты + отзывы за сутки + просрочки |
| Acknowledge | `main_ack.py` | CLI: список алертов / пометить как "в работе" |

Инфраструктура (`core/`: db, config, env, llm_provider) и точки входа (`main_*.py`,
в корне) — см. "Структура кода" в README.md.

LLM-провайдер — `core/llm_provider.py`, единая точка переключения (сейчас ProxyAPI/gpt-4o-mini).

## Домашка Zerocoder — требования выполнены

Задание: цель + набор задач + набор агентов + запуск + скриншоты. Всё это есть и с запасом —
5 работающих агентов на **реальных данных** (не выдуманный пример), с живыми скриншотами
из Telegram. Homework closed. Дальше — уже про реальный пилот клиенту, не про сдачу курса.
(Важно не путать эти два разных "MVP" — ниже про пилот.)

## Статус MVP (для реального пилота клиенту)

| Пункт | План | Факт |
|---|---|---|
| Сбор с площадок | 1-2 площадки | ✅ 3, все реально работают — 2ГИС через Apify (проверено на реальных данных, 10/10 новых) |
| Теги/категории | fixed 5-6 штук | ✅ переросло в управляемый словарь с описаниями — сверх плана |
| Пороги алертов | один хардкод-набор | ✅ из `client_config.yaml` |
| Watchdog "не ответил" | делаем | ✅ есть |
| Tone of voice | сценарий "документ" | ✅ есть (методички OpenUp «Директор ФК») |
| Persistent-алерты + упрощённый дайджест | без кнопок, но с ежедневной сводкой | ✅ `main_digest.py` — все открытые алерты (не только сегодняшние изменения) + счёт отзывов за сутки + просрочки. Acknowledge — `main_ack.py` (CLI, без кнопок, как и планировали для MVP) |

**MVP для пилота закрыт полностью.**

## Мультиклиентность

- [x] `client_config.yaml` реорганизован на 3 явных блока: **КЛИЕНТ** (меняется под
  каждого нового заказчика — локации, площадки, тон, telegram), **ШАБЛОН ПОД НИШУ**
  (теги/категории — переиспользуется для той же отрасли, полностью переписывается для
  другой), **ДВИЖОК** (алерты/SLA/таймауты — разумные дефолты, донастраиваются по факту
  реальных данных нового клиента, как делали для World Class). Чек-лист разворачивания —
  в шапке файла и в README
- [x] **Multi-tenant рефакторинг (2026-07-08)** — один код, `clients/<slug>/` на каждого
  клиента (конфиг/тон/БД/.env), выбор через обязательный `CLIENT_SLUG`. См. CHANGELOG
  [2026-07-08]. Это была теоретическая гипотеза — теперь код физически поддерживает
  несколько клиентов одновременно, не только "можно скопировать репозиторий"
- [ ] **Проверяется на практике прямо сейчас** — второй клиент, Даудель Спорт (Тюмень),
  `clients/daudelsport/`. Каркас конфига собран, площадки найдены (2ГИС/Яндекс.Карты/Zoon),
  но ещё НЕ пройден полный цикл: нет Telegram-бота, нет `tone_of_voice.md` (заглушка), не
  было ни одного реального прогона `main_collect.py`. Следующий шаг — довести до первого
  реального сбора и калибровки тегов, тогда гипотеза "15-20 минут на разворот" будет
  подтверждена или опровергнута фактически

## Ближайшее (не про код — про запуск пилота)

- [x] Задеплоено на VPS (2026-07-07) — `/home/deploy/bots/review-monitor/`, systemd-таймеры
  (пайплайн каждые 6ч + дайджест в 09:00 МСК по `poll_interval_hours`/`digest_time` из конфига),
  прогнано вручную через systemd, сообщения подтверждённо дошли в Telegram. Детали — CHANGELOG
  [2026-07-07]
- [ ] Дальше — наблюдать за первым реальным (не ручным) срабатыванием таймера, проверить, что
  всё идёт само по расписанию без вмешательства

## Золотой стандарт для Sentiment Analyst (2026-07-15)

- [x] Раздельная архитектура по клиентам (2026-07-15) — `tests/golden/dataset_worldclass.yaml`
  (19 кейсов) и `tests/golden/dataset_daudelsport.yaml` (18 кейсов), каждый клиент в
  полностью изолированном файле, без общего `dataset.yaml` со смешанными кейсами.
  Причина: Жорж — "надо раздельные делать, чтоб можно было зайти и запустить тест на
  конкретного клиента, а не корректировать эвал каждый раз под того клиента на кого
  мы хотим сделать запуск"; дерево тегов у клиентов разное (лежит в
  `clients/<slug>/client_config.yaml`), общий датасет вынуждал бы либо дублировать
  структуру, либо подгонять эвал под конкретный запуск. Новый клиент из другой ниши
  (например, мед. клиника) получает собственный `dataset_<slug>.yaml` с нуля — общий
  код (`run_eval.py`) не трогается.
  Кейсы отобраны под конкретные "решающие узлы": обычный позитив/негатив, скрытая
  проблема (высокий рейтинг + негативный тег внутри — самый ценный кейс, "золото", на
  которое мало кто обращает внимание), многотемные смешанные отзывы, нейтральный тон
  без явной жалобы/похвалы, составной отзыв (дописан спустя время — см. wc-108,
  сильный сигнал для сервиса "выйти на связь"), короткие/malformed без конкретики
  (осознанно оставлены — тоже реальный сигнал сервису, не мусор для выкидывания).
  Поводом послужил реальный баг (2026-07-15) — позитивный отзыв получил internal_note
  "3-я жалоба на персонал" (см. CHANGELOG).
- [x] `tests/golden/run_eval.py` — принимает `client_slug` аргументом командной строки
  (`run_eval.py worldclass`), загружает только соответствующий `dataset_<slug>.yaml`
  и словарь тегов/категорий из `clients/<slug>/client_config.yaml`. Сравнивает
  `sentiment` (точное совпадение) и `expected_tags` (тег должен найтись среди
  фактических с той же tag_sentiment; лишние теги сверх ожидаемых не считаются
  ошибкой). Отдельно ловит "галлюцинацию тега" — тег вне активного словаря клиента,
  но с `is_new: false` от модели (см. ниже). НЕ часть `pytest tests/` — платные
  вызовы LLM, не детерминировано, гонять вручную при смене модели/промпта/провайдера,
  требует `CLIENT_SLUG=<slug>` в окружении (используется `core/env.py`), не на
  каждый коммит.
- [x] Порог принятия — 90% (Жорж, 2026-07-15): "остальные 10% — сигнал, чтоб мы
  посмотрели и докрутили, возможно вскроется какая-то проблема", не жёсткий фейл
- [x] Первый прогон против реальной модели (2026-07-15): worldclass — sentiment 100%
  (19/19), tags 53% (10/19), общий accuracy 76%. daudelsport — sentiment 94% (17/18),
  tags 67% (12/18), общий accuracy 81%. Оба клиента НИЖЕ порога 90% — разобрать
  причины (см. ниже), не паниковать. Основные источники расхождений: (1) вариативность
  выбора тега моделью среди семантически пересекающихся тегов конфига (например
  "информирование" не находится там, где ожидали — вероятно ушло в другой тег);
  (2) галлюцинации тегов вне словаря с `is_new: false` — уже найдены "зона клуба",
  "вентиляция", "гардероб" (worldclass), "инструкторы" (daudelsport, притом что
  правильный тег "тренеры" уже есть в словаре — модель придумала синоним вместо
  использования существующего тега); (3) daudel-8 — sentiment регрессия positive→neutral
  между ручным дебаг-запуском и batch-прогоном.
- [x] **temperature=0** (2026-07-15, было 0.1) — `agents/sentiment_analyst.py`. Убрано
  как источник шума, где это возможно. НО: подтверждено на практике, что `gpt-4o-mini`
  (текущий провайдер, см. `core/llm_provider.py`) даже при temperature=0 не даёт
  100% детерминизма — это документированное ограничение OpenAI (MoE-архитектура,
  недетерминированность на уровне infra, не только семплирования). Пример: изолированный
  вызов `analyze_review()` на тексте wc-108 не дал галлюцинации тега "сервис", тот же
  текст внутри batch-прогона `run_eval.py` — дал. Вывод: небольшой процент "шума" между
  прогонами golden eval остаётся ожидаемым и НЕ является багом кода — не гнаться за
  устранением последних процентов через промпт, если расхождение единично и не
  воспроизводится стабильно.
- [x] worldclass после tag+zone + правок промпта/конфига (2026-07-15): tags 42-58%
  (плавает между прогонами из-за недетерминированности выше), общий accuracy 71-79%.
  Часть кейсов в dataset_worldclass.yaml обновлена под новую архитектуру tag+zone
  (см. коммент "ОБНОВЛЕНО (2026-07-15, tag+zone)" в файле) — там, где старое ожидание
  требовало тега-зоны вместо тега-темы. Остаточные легитимные расхождения (не чинить
  агрессивно, это вариативность интерпретации, не баг):
  - тег "информирование" системно проигрывает конкурирующему тегу темы, даже после
    явного правила "ставь ДОПОЛНИТЕЛЬНО к тегу темы" в промпте — исправилось частично
    (2 из 3 кейсов), не полностью
  - "персонал" vs "администраторы"/"тренеры" — модель предпочитает конкретную роль
    общему тегу, разумно, но расходится с частью ручной разметки
  - "раздевалка" тональность в wc-240 неустойчива между прогонами
- [x] **Эксперимент с усложнением промпта (2026-07-15) — ОТКАЧЕН, задокументирован как
  негативный результат**: пробовали (1) переключить провайдера на `gpt-4o` (полный)
  вместо `gpt-4o-mini` — смешанный результат, не однозначное улучшение (sentiment
  лучше на одном клиенте/хуже на другом, tags хуже на обоих), плюс стратегически не
  годится: если клиент потребует закрытый контур с локальной моделью (реалистичный
  сценарий для клиники, см. LEADS.md), доступа к топовым моделям не будет вообще —
  вернули mini. (2) добавили поле `"reasoning"` в JSON-схему aspects (мини-CoT внутри
  структурированного вывода, до финального tag) — ИЗМЕРЕННАЯ РЕГРЕССИЯ: daudelsport
  72%→61% (изолированный тест, без единого few-shot примера, чистый эффект поля).
  (3) per-client few-shot примеры (`clients/<slug>/few_shot_examples.yaml`,
  инфраструктура: `core.config.load_few_shot_examples`, `{{examples_block}}` в
  SYSTEM_PROMPT) — и generic-набор (общий для обоих клиентов), и specific-набор (2
  примера под реальные путаницы worldclass) ОБА дали регрессию (79%→63%, затем
  68% против чистого 71-79% baseline). Причина непонятна до конца — гипотеза:
  добавление структурной сложности (лишние поля, лишний текст в промпте) для
  `gpt-4o-mini` в этой конкретной задаче (27 тегов, тонкие границы) не даёт модели
  "больше подумать", а размывает внимание/анкорит на деталях примеров там, где
  паттерн не 1:1 применим. Откачено всё, кроме инфраструктуры (пустой
  `few_shot_examples.yaml` = `[]`, `load_few_shot_examples` возвращает `[]` для
  клиента без файла) — если в будущем найдётся формулировка few-shot, которая
  РЕАЛЬНО поднимает accuracy на golden eval (не в теории, а по факту), включать
  через этот же механизм, курировать по 1 примеру за раз с проверкой eval до/после,
  не насыпать пачками.
- [x] **Найден реальный ответ на "где висит пробел" (2026-07-15)**: пробел был не
  в понимании модели, а в **методике измерения**. Старая метрика `tags_correct` в
  `run_eval.py` считала "весь отзыв идеально или нет" (all-or-nothing per review) —
  на многотемных отзывах (7-9 ожидаемых тегов у wc-240/daudel-32) один пропущенный
  тег из 7 топил ВЕСЬ отзыв как полный провал, наравне с 0 из 7 правильных.
  Пересчитали per-tag (каждый ожидаемый тег отдельно, не весь отзыв целиком) —
  реальный recall оказался **76-82%**, а не 42-58%, которые показывала старая
  метрика. `run_eval.py` переписан: per-tag — основная метрика (используется для
  решения по порогу 90%), per-review — только для наглядности ("сколько отзывов
  разобраны идеально"), не для решения. `_compare_tags()` заменила `_tags_match()`.
- [x] **Baseline на новой метрике, конец сессии 2026-07-15** (tag+zone +
  temperature=0, mini, без reasoning/few-shot): worldclass — sentiment 100%
  (19/19), per-tag recall 82% (58/71), per-tag+sentiment 79% (56/71), общий
  accuracy (sentiment+per-tag) **89%**. daudelsport — sentiment 94% (17/18),
  per-tag recall 81% (46/57), per-tag+sentiment 79% (45/57), общий accuracy
  **87%**. Оба ВПЛОТНУЮ у порога 90%, не в разы ниже — принципиально другая
  картина, чем казалось раньше. Остаточные расхождения — не тревожные (см.
  список выше: вариативность интерпретации на пересекающихся темах,
  "информирование" системно проигрывает, единичные галлюцинации). Следующий шаг —
  не паниковать и не переусложнять промпт дальше, а точечно доразобрать оставшиеся
  10-15% расхождений (см. список задач ниже), это уже тонкая калибровка, не
  структурная проблема.
- [x] **Перепроверка few-shot на правильной метрике (2026-07-16)**: Жорж резонно
  спросил — а не был ли вывод "few-shot регрессирует" (см. выше, 2026-07-15)
  артефактом СЛОМАННОЙ all-or-nothing метрики, раз её потом заменили на per-tag?
  Вернул те же 2 примера в `clients/worldclass/few_shot_examples.yaml`, прогнал на
  НОВОЙ (правильной) метрике: 91%→81% — sentiment просел 100%→89% (2 новые ошибки:
  wc-95, wc-241), per-tag correct упал 79%→73%. Регрессия ПОДТВЕРДИЛАСЬ — это не
  артефакт измерения, few-shot реально вредит на этой модели/задаче даже при
  честном подсчёте. Вернул `few_shot_examples.yaml` к `[]`. worldclass снова 91%
  (выше порога 90%) без few-shot.
- [x] **worldclass пересёк порог 90%** (2026-07-16, чистый baseline: tag+zone,
  temperature=0, mini, без reasoning/few-shot) — общий accuracy **91%**
  (sentiment 100%, per-tag recall не пересчитан отдельно в этом прогоне, но
  общий выше порога). daudelsport остаётся на ~87% — не проверялся повторно
  после этого прогона, следующий шаг при возврате к теме.
- [ ] Датасет на 37 кейсов, не 50 — можно донабрать при следующей калибровке (Reply
  Strategist ещё не покрыт золотым стандартом вообще, только Sentiment Analyst)
- [ ] **Градация тональности тега** — см. отдельный пункт в V2 ниже, найдено при разборе
  датасета (wc-701: "далеко только" — не явный негатив, а лёгкая грусть без жалобы)
- [ ] Следующий шаг: разобрать расхождения tags по обоим клиентам вручную — отличить
  реальные ошибки модели от законной вариативности интерпретации (когда экспертная
  разметка и модель оба по-своему правы, но выбрали разные теги из пересекающихся по
  смыслу). Возможно, часть expected_tags в датасете нужно смягчить до "один из
  нескольких допустимых", а не жёсткое единственное ожидание.

## Approval новых тегов через Telegram (2026-07-15, предложено Жоржем, не в плане)

- [ ] Сейчас `main_analyze.py:60-71` при обнаружении тега вне активного словаря
  (`tag not in active_tags`) молча делает `insert_tag_if_new` — новый тег сразу
  становится частью словаря клиента, без подтверждения человеком. Это ровно то
  место, где могут просочиться галлюцинации модели (см. прогон golden dataset выше:
  "зона клуба", "вентиляция", "гардероб" у worldclass, "инструкторы" у daudelsport)
  — тихо засоряют словарь клиента ложными тегами.
- [ ] Идея Жоржа: вместо auto-insert — уведомление в Telegram с предложением тега
  ("предлагаю тег X в категории Y, вот почему") и подтверждение/отклонение владельцем
  до того, как тег попадёт в активный словарь.
- [ ] Не реализовано, требует отдельного плана до старта работы (архитектурное
  решение, не мелкая правка): нужен статус у тега (`pending`/`active`) или отдельная
  таблица `pending_tags`; Telegram-хендлер на approve/reject (аналог уже существующей
  инфраструктуры кнопок — см. `agents/notifier.py: repeat_offender_ack_keyboard`,
  `get_updates`, `answer_callback_query` в V2 ниже, но там их использование тоже
  приостановлено из-за polling-задержки); что делать с ТЕКУЩИМ отзывом, пока тег
  висит на подтверждении — размечать сразу с пометкой pending или ждать решения.

## Tag + Zone: разделить ЧТО от ГДЕ (2026-07-15, в работе)

- [ ] Проблема: один тег на аспект смешивает тему ("оборудование") и место
  ("тренажёрный зал"). Модель произвольно выбирает одно ИЛИ другое — одна и та же
  тема ("оборудование хромает") размазывается по разным зональным тегам от отзыва
  к отзыву, Alert Engine никогда не набирает порог по реальной проблеме. Пример
  Жоржа: "оборудование супер в тренажёрке" — тег должен быть "оборудование"
  (посыл к оборудованию), зона "тренажёрный зал" — уточнение, а не замена тега;
  иначе жалобы на оборудование в разных зонах (бассейн/ГП/тренажёрка) никогда не
  сойдутся в одну тему на дашборде.
- [ ] Решение — шаг 1 (это): добавить необязательное поле `zone` рядом с `tag`.
  Тег остаётся тем, что копит Alert Engine (пороги/алерты по темам не трогаем).
  Zone — доп.контекст для ручного разбора ("покажи все жалобы на оборудование —
  в какой зоне больше всего"). Реализация:
  1. `db/schema.sql` — `review_tags.zone TEXT` (nullable)
  2. `core/db.py: _migrate()` — `ALTER TABLE review_tags ADD COLUMN zone TEXT` для
     существующих БД клиентов (паттерн как у остальных добавленных колонок)
  3. `core/db.py: insert_review_tag(...)` — новый keyword-параметр
     `zone: str | None = None` (дефолт — не ломает существующие вызовы в тестах)
  4. `core/db.py: get_review_tags(...)` — добавить `zone` в SELECT и возвращаемый словарь
  5. `agents/sentiment_analyst.py` — расширить JSON-схему промпта: `"zone": "..." | null`
     на каждый aspect, инструкция модели сначала определить тему, затем отдельно —
     если из текста ясно место, куда это привязано, указать в zone; если место не
     ясно/не привязано к конкретной зоне — `zone: null`
  6. `main_analyze.py:71` — передать `aspect.get("zone")` в `insert_review_tag`
  7. Alert Engine (`get_negative_tag_events`, `recompute_all`,
     `get_top_tags_by_sentiment_since`) — НЕ трогаем, zone физически не попадает
     в их SELECT/группировку по tag
  8. Дашборд/Telegram/Reply Strategist — не трогаем в этом шаге, zone аддитивна,
     ничего не ломается, если её нигде не показывать
  9. golden dataset и `run_eval.py` — обновить под новую форму aspects (`zone` в
     ожидаемых данных) отдельным шагом после реализации, не одновременно
- [ ] Решение — шаг 2 (следующий, НЕ сейчас, зависит от результатов шага 1 на
  реальных данных): **зональный алерт**. Найдено при обсуждении с Жоржем
  (2026-07-15): управляющему клуба/клиники нужен не только "тема X копится"
  (уже есть), но и "зона Y стабильно генерирует негатив независимо от темы" —
  например тренажёрный зал получает то жалобы на оборудование, то на персонал,
  то на чистоту, по отдельности ни одна тема не набирает порог, но зона в целом
  проблемная (вопрос к супервайзеру ТЗ/фитнес-директору). Для клиники — аналогично:
  кабинет косметологии стабильно собирает разные виды негатива (то на сотрудника,
  то на оборудование). Это ОТДЕЛЬНАЯ логика от тегового алерта — свои
  пороги/окно (считает ЛЮБОЙ негативный тег, привязанный к зоне, группировка по
  `(zone, location_id)` вместо `(tag, location_id)`), своё Telegram-сообщение.
  Не проектировать заранее "вслепую" — сначала посмотреть на реальные данные zone
  после шага 1, решить какие пороги имеют смысл (тема и зона — разные по природе
  вещи, нельзя просто скопировать пороги тегового алерта).

## V2 (сознательно отложено, не входит ни в домашку, ни в MVP пилота)

- [x] **Repeat offenders (2026-07-14)** — реализовано. `agents/alert_engine.compute_repeat_offenders`
  + `recompute_repeat_offenders`, ключ `(author, platform, location_id)` — площадки НЕ
  смешиваются. Персистентность переиспользует таблицу `alerts` (новая колонка `alert_type`,
  ключ автора хранится в `tag` как `"author:{author}:{platform}"` — минимальная миграция без
  переименования существующих колонок, см. CHANGELOG). Порог — кастом на клиента
  (`alert_rules.repeat_offender` в конфиге). `main_repeat_offender_notify.py` — отдельный
  systemd timer раз в `renotify_after_days` (не в 6-часовом основном цикле)
- [ ] **Webhook-приёмник для inline-кнопок Telegram** — сейчас нет кнопки "Связался" у
  repeat-offender уведомлений: получатель (сервисная служба клиента) не технарь, `main_ack.py`
  (CLI) ей недоступен. Кнопка в Telegram технически готова в коде (`agents/notifier.py`:
  `repeat_offender_ack_keyboard`, `get_updates`, `answer_callback_query`,
  `remove_message_keyboard`; `main_repeat_offender_poll.py`), но НЕ используется — 2026-07-14
  живой тест показал, что polling (периодический опрос вместо мгновенной реакции) даёт
  заметную задержку между нажатием и исчезновением кнопки — плохой UX ("нажал — ничего не
  происходит"). Правильное решение — webhook: постоянно работающий HTTP-приёмник
  (Flask/gunicorn за nginx, тот же паттерн, что `landing-forms`), который Telegram сам
  дёргает мгновенно при нажатии. Это НЕ разовый скрипт, а новый долгоживущий процесс на
  VPS — делать вместе с доменом/лендингом ниже, не отдельным патчем. Временная мера сейчас:
  `alert_rules.repeat_offender.max_notifications` — напоминает N раз и замолкает само
- [x] **Домен + авторизация + список клиентов (2026-07-14)** — куплен `reviewpulse.ru`,
  SSL через Let's Encrypt (автопродление), `webapp/` (Flask+gunicorn+nginx, постоянный
  systemd-процесс — первый в проекте, весь остальной код это батч-скрипты по расписанию).
  Логин через `flask-login`, роли `admin` (видит всех `clients/*`) / `client` (только свой
  `client_slug`), отдельная `webapp/users.db` — не смешана с клиентскими БД отзывов.
  Принудительная смена пароля при первом входе (`must_change_password`, перехват в
  `before_request` — нельзя обойти прямой ссылкой). Пользователей заводит Жорж вручную
  через `webapp/create_user.py` (CLI), самостоятельной регистрации нет и не планируется
  для текущего масштаба (несколько клиентов, не self-serve SaaS)
- [ ] Лендинг на `reviewpulse.ru` (для холодных лидов с Kwork/переговоров) — не начат,
  сейчас на домене сразу открывается `/login`. Не приоритет, пока не появится трафик,
  которому есть смысл показывать лендинг раньше формы входа
- [ ] Дашборд конкретного клиента — см. отдельный пункт "Дашборд v1/v2" ниже
- [ ] Webhook для inline-кнопок Telegram (пункт выше про repeat offender) — теперь
  инфраструктура для этого есть (`webapp/` уже постоянный процесс), реализация кнопки
  всё ещё не сделана
- [ ] **Flamp и Spravka.ru для Даудель Спорт** (2026-07-08, отложено — не основные площадки):
  Жорж прислал прямые ссылки — [Flamp](https://tyumen.flamp.ru/firm/daudel_sport_fitnes_klub-1830116630156385#reviews),
  [Spravka.ru](https://tyumen.spravka.ru/fitnes-kluby/fitnes-klub-daudel-sport/otzivi-ocenki-reiting).
  Нужны отдельные коллекторы — ни один из двух пока не поддержан в `collectors/`. У WorldClass
  Flamp сознательно не подключался (дублирует 2ГИС, общий владелец + 403 на прямой доступ) —
  для Даудель Спорт эта логика не проверена, возможно отзывы отличаются от 2ГИС
- [ ] **Analytics Agent** — NL-запрос → фильтр → SQL. Ценность, когда клиент уже пользуется
  инструментом и хочет копать статистику сам — апсейл после пилота, не входной билет
- [ ] `discover_new_platforms` — еженедельный поиск новых площадок отзывов
- [ ] UI утверждения новых тегов (сейчас вручную через SQL)
- [ ] Динамический tone-of-voice miner (когда у клуба накопится история ответов)
- [ ] **Градация тональности тега** (обсуждение 2026-07-15, при разборе золотого
  стандарта) — сейчас `tag_sentiment` строго `positive|neutral|negative`, три
  значения. Найден пограничный случай (wc-701: "Хороший клуб, далеко только" —
  5★, тег "локация"): Жорж считает это не полноценным негативом, а лёгкой грустью
  без явной жалобы — "сигнал для сервиса, что это может быть ограничивающим
  фактором для продаж, стоит усилить какими-то плюшками", но не тема, которую
  реально "чинить" как проблему. Текущая 3-значная шкала не различает "явная
  жалоба" от "мягкое сожаление без претензии". Решение сейчас: оставить как
  `negative` (пока не блокирует работу), но зафиксировать для будущего —
  возможно нужна 4-5-балльная шкала интенсивности тега (по аналогии с
  `sentiment_score` у отзыва целиком), а не только знак. Требует: пересмотр
  промпта Sentiment Analyst, миграция схемы `review_tags` (новое поле), пересчёт
  влияния на Alert Engine (сейчас burst/chronic считают per-тег без интенсивности)

### Дашборд клиента — v1 (в разработке) и v2 (отложено)

Спроектирован на основе двух référence-документов от Жоржа (2026-07-14, сгенерированы
Perplexity — управленческая архитектура review-дашборда для руководителя сервиса).
Решение: v1 — только на данных, которые уже реально считаются в системе, никаких новых
аналитических функций/LLM-агентов. Всё, что требует новой логики — в v2, промониторить
использование v1 и решить, что реально нужно клиентам, а не проектировать вслепую.

**v1 (эта итерация), вкладка "Обзор":**
- Сводка сверху: открытые тег-алерты + repeat-offender алерты + просрочки по SLA
- Тренд отзывов за месяц по тональности (🟢🟡🔴)
- Открытые тег-алерты списком (тема, severity, счётчик, окно)
- Открытые repeat-offender алерты списком (автор, площадка, счётчик)
- Топ-3 хвалимых / топ-3 ругаемых темы за месяц (`get_top_tags_by_sentiment_since`)
- Просрочки: недавние (`get_overdue_reviews`) отдельно от "забытых" 90-180 дней
  (`get_stale_overdue_reviews`) — разный тон, не мешать в одну кучу
- Сравнение площадок (2ГИС/Яндекс/Zoon) — объём и доля негатива, новый лёгкий SQL-запрос
- **Скрытые проблемы** — отзывы с высокой оценкой (4-5★), но с негативным тегом внутри
  (aspect-based теги это уже хранят, только нужен новый запрос) — из документа Perplexity
  ("Sentiment vs Rating mismatch"), сильный аргумент именно ПРО aspect-based подход проекта

**v1, вкладка "Лента отзывов":** список отзывов с фильтрами (площадка/тональность/период),
пагинация, текст+теги+черновик ответа+статус.

**Ручная коррекция тега (2026-07-16, предложено Жоржем — СДЕЛАНО, НЕ путать с
"Approval новых тегов" ниже — это другая задача):** владелец процесса видит в ленте
отзывов, что модель поставила неверный тег (живой пример — Wi-Fi-отзыв 2026-07-16,
модель дала "информирование"+"сервис" вместо "wifi"+"администраторы", см.
CHANGELOG), и может тут же вручную поменять тег на правильный из активного словаря
клиента — без обращения к разработчику. Решает то, что approval-флоу НЕ решает:
approval фильтрует вход НОВЫХ тегов в словарь, но не чинит выбор МЕЖДУ уже
одобренными тегами (путаница цена/возврат средств/администраторы/информирование,
разбор которой занимает большую часть калибровки, см. записи выше и CHANGELOG
2026-07-16). Побочный эффект, ради которого стоит делать: каждая ручная коррекция —
готовый сигнал для golden dataset/few-shot без разбора вручную, как сейчас.
Реализовано: `webapp/templates/dashboard_reviews.html` — клик ✎ на тег-чипе
превращает его в форму (select тега/тональности, сгруппированный по категории
через `<optgroup>` — плоский список 27+ тегов был неудобен, фидбек Жоржа 2026-07-16),
кнопка "+ тег" на карточке добавляет ДОПОЛНИТЕЛЬНЫЙ тег на отзыв, не меняя
существующие. Backend: `POST /dashboard/<slug>/tag/<tag_row_id>` (правка) и
`POST /dashboard/<slug>/review/<review_id>/tag` (добавление) в `webapp/app.py`,
`core.db.update_review_tag()` правит строку `review_tags` по её собственному id
(не по review_id+tag — один отзыв может законно иметь дубли тега на разные
цитаты, см. review 84 в CHANGELOG 2026-07-16). Оба роута валидируют тег СТРОГО
против активного словаря клиента — не путь для добавления новых тегов мимо
approval-флоу. Плюс кнопка "Пересчитать алерты сейчас" (`POST
/dashboard/<slug>/recompute-alerts`) — не ждать планового `main_alerts.py`
после массовой правки, видит эффект сразу, использует те же чистые функции
ядра (`agents.alert_engine.recompute_all`/`recompute_repeat_offenders`).
Задеплоено на VPS, проверено Жоржем вживую на reviewpulse.ru.

**Правки после первого визуального ревью (2026-07-14):**
- Карточки сводки сверху не кликабельны (клик просто скроллил к тому же контенту ниже —
  бесполезная псевдо-интерактивность), сами карточки алертов/офендеров кликабельны и
  открывают модалку с drill-down до сырых отзывов (см. следующий пункт)
- Порядок секций: "Просрочки по SLA" сразу после "Открытых алертов" (было в самом низу)
- Подписи карточек — "Критично"/"Внимание" вместо "Красных алертов"/"Жёлтых алертов":
  цвет — деталь реализации, не смысл для владельца бизнеса

**Запрос на доработку блока "Отзывы за N дней" (2026-07-14):** сейчас это только
статус-бар (полоска 🟢🟡🔴 с текущими цифрами за фиксированные 30 дней, без истории).
Жорж хочет график динамики тональности с выбором периода (неделя/месяц/квартал/своя
дата), а статус-бар — под графиком, как текущий срез итогов за выбранный период (не
отдельно от графика, а его "итоговая строка"). Требует: (1) агрегация отзывов по
дням/неделям для графика — новый SQL-запрос с группировкой по периоду, которого сейчас
нет (`get_review_sentiment_counts_since` даёт только один агрегат за весь период, не
временной ряд), (2) UI-контрол выбора периода, (3) сама отрисовка графика — решить,
через что (см. пункт про иерархический дашборд ниже, тот же открытый вопрос про
библиотеку/canvas). Логично делать вместе с графиком динамики по категориям/тегам
из следующего пункта — одна и та же инфраструктура (агрегация по времени + рендер),
не две отдельные задачи

**v2 — отложено, требует новой разработки (не просто SQL-запрос к тому, что уже считается):**
- **Health Score (0-100)** — единая формула-агрегатор многих сигналов в одно число.
  Решить формулу отдельно, не с ходу — задача дизайна метрики, не кода
- **Auto-кластеризация жалоб без ручных тегов** (embeddings+clustering) — у нас уже есть
  управляемый словарь тегов через LLM, это другой подход поверх работающего — не очевидно
  нужен, конкурирует сам с собой
- **Root cause hypotheses / Executive Summary текстом** — новый LLM-вызов поверх кластера
  жалоб, отдельный агент, стоит денег на каждый запуск — не бесплатно, взвесить ценность
- **Anomaly detection** (всплеск за 24-72 часа, не 30/90 дней как burst сейчас) — новая
  метрика, у нас есть похожее (burst/chronic), но не такое короткое окно
- **Before/After sentiment recovery** — сопоставить дату ответа компании с тональностью
  последующих отзывов той же категории — отдельная аналитическая функция, которой нет
- **Emerging topics** (тема, которой не было месяц назад) — diff тегов между двумя
  периодами, несложно, но не реализовано
- **Детекция накрутки/ботов/дублей** — другая эвристика, чем repeat offender (паттерны
  текста, скорость публикации, отсутствие истории у автора) — не путать с уже готовым
  repeat offender, это разные задачи
- **VIP/LTV клиентов** — нет данных о ценности клиента вообще, потребовалась бы интеграция
  с CRM клиента — вне текущего скоупа продукта
- **Полная ролевая модель** (owner/service head/operational manager/branch manager/analyst
  из документов) — сейчас 2 роли (admin/client), под 5 ролей с разными срезами нужна
  куда более сложная модель прав. Делать, когда появится клиент с реальной оргструктурой,
  которой это нужно — оба текущих клиента (World Class, Даудель Спорт) однолокационные
- **Heatmap филиалы×темы** — оба текущих клиента одноточечные, `location_id` в схеме уже
  есть на будущее, но фича непроверяема на реальных данных, пока нет мультиточечного клиента
- **Sentiment vs Rating mismatch как отдельный AI-детектор** — v1 уже покрывает базовый
  случай через блок "Скрытые проблемы" (см. выше), развитая версия с AI-объяснением почему
  — v2
- **Иерархический дашборд по категориям тегов** (обсуждение 2026-07-14, после разбора
  ложного red-алерта "услуги" — см. CHANGELOG) — сейчас теги показываются плоским
  списком, категория (из `category_dictionary`) нигде не отображается, хотя в БД уже
  есть. Идея Жоржа: категория ("тренажёрный зал", "коммерция" и т.п.) — сворачиваемый
  верхний уровень с агрегированным счётчиком, разворачивается в конкретные теги внутри
  (частая проблема с широкими тегами — см. "услуги" — в том, что нельзя понять суть,
  не читая все отзывы; иерархия category→tag частично решает без AI-кластеризации,
  раз структура уже задана вручную в конфиге). Плюс график динамики жалоб по
  категории/тегу за месяц/год — сейчас нет вообще никакой временной визуализации,
  только текущий срез. Требует: новый агрегирующий SQL-запрос (группировка по
  category+tag с count), новый UI-компонент (collapsible tree или похожее), выбор
  библиотеки для графика (или простой SVG/canvas без зависимостей — не решено)

## Идеи (не триажены, просто чтобы не забыть)

- Docker Compose обёртка — **решено: не сейчас.** Окупается при 2-3+ параллельных клиентах (шаблон деплоя), не при одном пилоте. Деплой на VPS — тем же паттерном systemd+venv, что telegram-bot/booking-bot. Вернуться к вопросу, когда появится второй клиент

## Известные ограничения

- ~~2ГИС не собирается напрямую~~ — **решено.** Apify (`collectors/twogis.py`, actor `zen-studio/2gis-reviews-scraper`, residential-прокси) — проверено на реальных данных, 9 из 10 отзывов на 2ГИС уже с ответом клуба (в отличие от Яндекс.Карт и Zoon — полезный факт для питча). $1/1000 отзывов в выдаче, наш прогон на 10 отзывов стоил $0.017
- Официальный API 2ГИС не отдаёт текст отзывов вообще (только через отдел продаж, неясные условия) — не вариант
- 2ГИС в пользовательском соглашении (п. 2.13) прямо запрещает автоматический сбор данных — не проблема для внутреннего мониторинга своего клиента, но не афишировать механику, если это станет публичным продуктом
- На Яндекс.Картах и Zoon клуб почти не отвечает на отзывы (1 из 52) — но на 2ГИС всё наоборот, 9 из 10 с ответом. tone_of_voice по-прежнему гипотеза (мало реальных образцов даже с учётом 2ГИС), но неравномерность активности по площадкам — сама по себе полезный факт для питча
- "Чёрная реклама"/"троллинг" сознательно не классифицируются автоматически — риск ошибочно обидеть реального клиента выше пользы от автоматизации (см. `tone_of_voice.md`)

## Расширение воронки

- [x] Черновик кворка для Kwork.ru готов (текст передан Жоржу напрямую, **не хранится
  в этом репозитории** — коммерческое предложение/цены не место в публичном коде).
  Ниша — любой бизнес с отзывами (не только фитнес), 3 тарифа по глубине
  внедрения (аудит / мониторинг+алерты / полный цикл с черновиками ответов)
- [ ] Не опубликовано — публикация вручную через личный кабинет Kwork (не
  автоматизируется), + нужны скриншоты Telegram-уведомлений с замазанными
  ФИО клиентов клуба (сырые скрины ещё не сделаны)
- [ ] Не протестировано спросом — Kwork это холодный доп.канал, не основной
  трек к 300к (сеть Resulting приоритетнее), не наращивать сюда время сверх
  однократной публикации, пока нет заказов

## Публикация

- [x] Аудит на приватные данные (2026-07-07): секретов/токенов в коде нет, `.env` и
  `db/reviews.db` в `.gitignore` и не попали в коммит, реальных ФИО клиентов/телефонов
  в markdown/конфиге не найдено. Адрес и название клуба — публичная информация (сам клуб
  как пилотный кейс тоже публичен, см. CLAUDE.md)
- [x] Опубликовано: https://github.com/GerogeousGT/review-monitor (публичный репозиторий)

## Связанные файлы

- [README.md](README.md) — что это, как установить, статус компонентов
- [CHANGELOG.md](CHANGELOG.md) — история сессий, технические решения и почему
- [tone_of_voice.md](tone_of_voice.md) — гайд тона для Reply Strategist
- [client_config.yaml](client_config.yaml) — все настройки под клиента
