# PLAN — review-monitor

Живой документ: что задумали, что сделали, что в очереди. Обновляется по ходу работы —
не только "после того как сделали", но и в момент, когда придумали что-то новое и решили
отложить. Цель — не терять идеи и не путать "мы это обсуждали" с "мы это построили".

## Что это

Мультиагентный мониторинг отзывов для сервисного бизнеса. Домашка курса Zerocoder
(автономные/многоагентные системы), собирается сразу как реальный демо-кейс на примере
фитнес-клуба (World Class, Тюмень).

**Стратегический разворот (2026-07-06):** не только пилот для одного клиента — план
докрутить до аналитики и быстро разворачивать на несколько компаний (прислать им готовый
анализ отзывов как демо/лид-магнит). Отсюда — приоритет на быстрое пересобирание конфига
под нового клиента (см. ниже) раньше, чем на Docker (Docker — когда клиентов станет 2-3+,
см. "Идеи").

## Архитектура (5 компонентов)

```
[Scheduler] → [Collector] → [Sentiment Analyst] → [Reply Strategist]
                                    ↓
                            [Alert Engine] → [Notifier → Telegram]
```

| Компонент | Файл | Что делает |
|---|---|---|
| Collector | `collectors/*.py`, `main_collect.py` | Playwright, курсор по последнему отзыву, не пересканирует историю |
| Sentiment Analyst | `agents/sentiment_analyst.py`, `main_analyze.py` | LLM, aspect-based тональность, управляемый словарь тегов+категорий |
| Alert Engine | `agents/alert_engine.py`, `main_alerts.py` | Пороги по окнам ("вспышка") + ступенчатая эскалация ("тлеющая проблема", 180→360→720 дней), персистентное состояние, Watchdog по SLA |
| Reply Strategist | `agents/reply_strategist.py`, `main_reply.py` | Черновик ответа + классификация, гайд тона (`tone_of_voice.md`) |
| Notifier | `agents/notifier.py`, `main_notify.py` | Telegram, свой бот @RewvieWClass_bot |
| Дайджест | `main_digest.py` | Ежедневная сводка: все открытые алерты + отзывы за сутки + просрочки |
| Acknowledge | `main_ack.py` | CLI: список алертов / пометить как "в работе" |

Инфраструктура (`core/`: db, config, env, llm_provider) и точки входа (`main_*.py`,
в корне) — см. "Структура кода" в README.md.

LLM-провайдер — `core/llm_provider.py`, единая точка переключения (сейчас ProxyAPI/gpt-4o-mini).

## Домашка Zerocoder — требования выполнены

Задание: цель + набор задач + набор агентов + запуск + скриншоты. Всё это есть и с запасом —
5 работающих агентов на **реальных данных** (не выдуманный пример), с живыми скриншотами
из Telegram. Homework closed. Дальше — уже про реальный пилот клиенту, не про сдачу курса.
(Важно не путать эти два разных "MVP" — ниже про пилот.)

## Статус MVP (для реального пилота клиенту)

| Пункт | План | Факт |
|---|---|---|
| Сбор с площадок | 1-2 площадки | ✅ 3, все реально работают — 2ГИС через Apify (проверено на реальных данных, 10/10 новых) |
| Теги/категории | fixed 5-6 штук | ✅ переросло в управляемый словарь с описаниями — сверх плана |
| Пороги алертов | один хардкод-набор | ✅ из `client_config.yaml` |
| Watchdog "не ответил" | делаем | ✅ есть |
| Tone of voice | сценарий "документ" | ✅ есть (методички OpenUp «Директор ФК») |
| Persistent-алерты + упрощённый дайджест | без кнопок, но с ежедневной сводкой | ✅ `main_digest.py` — все открытые алерты (не только сегодняшние изменения) + счёт отзывов за сутки + просрочки. Acknowledge — `main_ack.py` (CLI, без кнопок, как и планировали для MVP) |

**MVP для пилота закрыт полностью.**

## Мультиклиентность

- [x] `client_config.yaml` реорганизован на 3 явных блока: **КЛИЕНТ** (меняется под
  каждого нового заказчика — локации, площадки, тон, telegram), **ШАБЛОН ПОД НИШУ**
  (теги/категории — переиспользуется для той же отрасли, полностью переписывается для
  другой), **ДВИЖОК** (алерты/SLA/таймауты — разумные дефолты, донастраиваются по факту
  реальных данных нового клиента, как делали для World Class). Чек-лист разворачивания —
  в шапке файла и в README
- [x] **Multi-tenant рефакторинг (2026-07-08)** — один код, `clients/<slug>/` на каждого
  клиента (конфиг/тон/БД/.env), выбор через обязательный `CLIENT_SLUG`. См. CHANGELOG
  [2026-07-08]. Это была теоретическая гипотеза — теперь код физически поддерживает
  несколько клиентов одновременно, не только "можно скопировать репозиторий"
- [ ] **Проверяется на практике прямо сейчас** — второй клиент, Даудель Спорт (Тюмень),
  `clients/daudelsport/`. Каркас конфига собран, площадки найдены (2ГИС/Яндекс.Карты/Zoon),
  но ещё НЕ пройден полный цикл: нет Telegram-бота, нет `tone_of_voice.md` (заглушка), не
  было ни одного реального прогона `main_collect.py`. Следующий шаг — довести до первого
  реального сбора и калибровки тегов, тогда гипотеза "15-20 минут на разворот" будет
  подтверждена или опровергнута фактически

## Ближайшее (не про код — про запуск пилота)

- [x] Задеплоено на VPS (2026-07-07) — `/home/deploy/bots/review-monitor/`, systemd-таймеры
  (пайплайн каждые 6ч + дайджест в 09:00 МСК по `poll_interval_hours`/`digest_time` из конфига),
  прогнано вручную через systemd, сообщения подтверждённо дошли в Telegram. Детали — CHANGELOG
  [2026-07-07]
- [ ] Дальше — наблюдать за первым реальным (не ручным) срабатыванием таймера, проверить, что
  всё идёт само по расписанию без вмешательства

## V2 (сознательно отложено, не входит ни в домашку, ни в MVP пилота)

- [ ] **Flamp и Spravka.ru для Даудель Спорт** (2026-07-08, отложено — не основные площадки):
  Жорж прислал прямые ссылки — [Flamp](https://tyumen.flamp.ru/firm/daudel_sport_fitnes_klub-1830116630156385#reviews),
  [Spravka.ru](https://tyumen.spravka.ru/fitnes-kluby/fitnes-klub-daudel-sport/otzivi-ocenki-reiting).
  Нужны отдельные коллекторы — ни один из двух пока не поддержан в `collectors/`. У WorldClass
  Flamp сознательно не подключался (дублирует 2ГИС, общий владелец + 403 на прямой доступ) —
  для Даудель Спорт эта логика не проверена, возможно отзывы отличаются от 2ГИС
- [ ] **Analytics Agent** — NL-запрос → фильтр → SQL. Ценность, когда клиент уже пользуется
  инструментом и хочет копать статистику сам — апсейл после пилота, не входной билет
- [ ] Веб-дашборд для собственника (Flask/FastAPI поверх той же БД)
- [ ] `discover_new_platforms` — еженедельный поиск новых площадок отзывов
- [ ] Полный state machine алертов с inline-кнопками в Telegram
- [ ] UI утверждения новых тегов (сейчас вручную через SQL)
- [ ] Динамический tone-of-voice miner (когда у клуба накопится история ответов)

## Идеи (не триажены, просто чтобы не забыть)

- Docker Compose обёртка — **решено: не сейчас.** Окупается при 2-3+ параллельных клиентах (шаблон деплоя), не при одном пилоте. Деплой на VPS — тем же паттерном systemd+venv, что telegram-bot/booking-bot. Вернуться к вопросу, когда появится второй клиент

## Известные ограничения

- ~~2ГИС не собирается напрямую~~ — **решено.** Apify (`collectors/twogis.py`, actor `zen-studio/2gis-reviews-scraper`, residential-прокси) — проверено на реальных данных, 9 из 10 отзывов на 2ГИС уже с ответом клуба (в отличие от Яндекс.Карт и Zoon — полезный факт для питча). $1/1000 отзывов в выдаче, наш прогон на 10 отзывов стоил $0.017
- Официальный API 2ГИС не отдаёт текст отзывов вообще (только через отдел продаж, неясные условия) — не вариант
- 2ГИС в пользовательском соглашении (п. 2.13) прямо запрещает автоматический сбор данных — не проблема для внутреннего мониторинга своего клиента, но не афишировать механику, если это станет публичным продуктом
- На Яндекс.Картах и Zoon клуб почти не отвечает на отзывы (1 из 52) — но на 2ГИС всё наоборот, 9 из 10 с ответом. tone_of_voice по-прежнему гипотеза (мало реальных образцов даже с учётом 2ГИС), но неравномерность активности по площадкам — сама по себе полезный факт для питча
- "Чёрная реклама"/"троллинг" сознательно не классифицируются автоматически — риск ошибочно обидеть реального клиента выше пользы от автоматизации (см. `tone_of_voice.md`)

## Расширение воронки

- [x] Черновик кворка для Kwork.ru готов (текст передан Жоржу напрямую, **не хранится
  в этом репозитории** — коммерческое предложение/цены не место в публичном коде).
  Ниша — любой бизнес с отзывами (не только фитнес), 3 тарифа по глубине
  внедрения (аудит / мониторинг+алерты / полный цикл с черновиками ответов)
- [ ] Не опубликовано — публикация вручную через личный кабинет Kwork (не
  автоматизируется), + нужны скриншоты Telegram-уведомлений с замазанными
  ФИО клиентов клуба (сырые скрины ещё не сделаны)
- [ ] Не протестировано спросом — Kwork это холодный доп.канал, не основной
  трек к 300к (сеть Resulting приоритетнее), не наращивать сюда время сверх
  однократной публикации, пока нет заказов

## Публикация

- [x] Аудит на приватные данные (2026-07-07): секретов/токенов в коде нет, `.env` и
  `db/reviews.db` в `.gitignore` и не попали в коммит, реальных ФИО клиентов/телефонов
  в markdown/конфиге не найдено. Адрес и название клуба — публичная информация (сам клуб
  как пилотный кейс тоже публичен, см. CLAUDE.md)
- [x] Опубликовано: https://github.com/GerogeousGT/review-monitor (публичный репозиторий)

## Связанные файлы

- [README.md](README.md) — что это, как установить, статус компонентов
- [CHANGELOG.md](CHANGELOG.md) — история сессий, технические решения и почему
- [tone_of_voice.md](tone_of_voice.md) — гайд тона для Reply Strategist
- [client_config.yaml](client_config.yaml) — все настройки под клиента
